# Local Multimodal RAG Pipeline Dependencies
# ============================================================

# Core dependencies
numpy>=1.24.0

# Text embedding (via vLLM API, no local model needed)
requests>=2.28.0

# OCR - PaddleOCR
paddleocr>=2.7.0
paddlepaddle-gpu>=2.5.0  # For GPU mode; use paddlepaddle for CPU-only

# PDF processing
pymupdf>=1.23.0  # fitz

# Image processing
Pillow>=10.0.0

# Office document processing
python-docx>=0.8.11
python-pptx>=0.6.21
openpyxl>=3.1.2
xlrd>=2.0.1  # For older .xls files

# Ebook processing
ebooklib>=0.18
mobi>=0.1  # For .mobi files
beautifulsoup4>=4.12.0  # For ebook HTML parsing

# Audio transcription
faster-whisper>=0.9.0  # CPU-based Whisper

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# === FastAPI Web Backend ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiosqlite>=0.19.0

# Media metadata extraction
pymediainfo>=6.0.0
tinytag>=1.10.0

# Optional: Archive handling (most systems have these tools)
# unrar, unzip, 7z are usually system packages

# Optional: djvu processing (requires system packages djvutxt, ddjvu)
# These are typically: djvulibre-bin (Ubuntu) or djvu (other distros)

# Optional: LibreOffice for .ppt/.doc conversion
# Typically installed as system package

# Optional: ffprobe for audio duration detection
# Typically part of ffmpeg system package

# Optional: whisper.cpp for faster transcription
# Requires manual installation from https://github.com/ggerganov/whisper.cpp

# Development/testing
pytest>=7.0.0  # Optional, for testing

# Note: vLLM is NOT a pip dependency - it runs as a separate service
# Install vLLM separately: pip install vllm
# And run: vllm serve ~/models/jina-embeddings-v5-text-small/ --port 8001 ...